diff --git a/src/channels/mod.rs b/src/channels/mod.rs
index b7712b7..dae541a 100644
--- a/src/channels/mod.rs
+++ b/src/channels/mod.rs
@@ -14,6 +14,8 @@
 //! To add a new channel, implement [`Channel`] in a new submodule and wire it into
 //! [`start_channels`]. See `AGENTS.md` §7.2 for the full change playbook.
 
+use chrono::TimeZone;
+
 pub mod acp_server;
 pub mod bluesky;
 pub mod clawdtalk;
@@ -2432,7 +2434,11 @@ async fn process_channel_message(
     };
 
     // Preserve user turn before the LLM call so interrupted requests keep context.
-    append_sender_turn(ctx.as_ref(), &history_key, ChatMessage::user(&msg.content));
+    let _ts = chrono::Local.timestamp_opt(msg.timestamp as i64, 0)
+        .single()
+        .unwrap_or_else(chrono::Local::now);
+    let _stamped_content = format!("[{}] {}", _ts.format("%Y-%m-%dT%H:%M:%S%:z"), msg.content);
+    append_sender_turn(ctx.as_ref(), &history_key, ChatMessage::user(&_stamped_content));
 
     // Build history from per-sender conversation cache.
     let prior_turns_raw = if force_fresh_session {
diff --git a/src/daemon/mod.rs b/src/daemon/mod.rs
index 036e81b..9384a61 100644
--- a/src/daemon/mod.rs
+++ b/src/daemon/mod.rs
@@ -1,6 +1,6 @@
 use crate::config::Config;
 use anyhow::Result;
-use chrono::Utc;
+use chrono::{Local, Utc};
 use std::future::Future;
 use std::path::PathBuf;
 use tokio::task::JoinHandle;
@@ -373,7 +373,7 @@ async fn run_heartbeat_worker(config: Config) -> Result<()> {
         let mut tick_had_error = false;
         for task in &tasks_to_run {
             let task_start = std::time::Instant::now();
-            let task_prompt = format!("[Heartbeat Task | {}] {}", task.priority, task.text);
+            let task_prompt = format!("[{}] [Heartbeat Task | {}] {}", chrono::Local::now().format("%Y-%m-%dT%H:%M:%S%:z"), task.priority, task.text);
             let prompt = match &session_context {
                 Some(ctx) => format!("{ctx}\n\n{task_prompt}"),
                 None => task_prompt,
diff --git a/src/gateway/mod.rs b/src/gateway/mod.rs
index dc476ec..df29825 100644
--- a/src/gateway/mod.rs
+++ b/src/gateway/mod.rs
@@ -20,6 +20,8 @@ pub mod static_files;
 pub mod tls;
 pub mod ws;
 
+use chrono::Local;
+
 use crate::channels::{
     session_backend::SessionBackend, session_sqlite::SqliteSessionBackend, Channel,
     GmailPushChannel, LinqChannel, NextcloudTalkChannel, SendMessage, WatiChannel, WhatsAppChannel,
@@ -1250,7 +1252,8 @@ async fn persist_pairing_tokens(config: Arc<Mutex<Config>>, pairing: &PairingGua
 
 /// Simple chat for webhook endpoint (no tools, for backward compatibility and testing).
 async fn run_gateway_chat_simple(state: &AppState, message: &str) -> anyhow::Result<String> {
-    let user_messages = vec![ChatMessage::user(message)];
+    let _stamped = format!("[{}] {}", chrono::Local::now().format("%Y-%m-%dT%H:%M:%S%:z"), message);
+    let user_messages = vec![ChatMessage::user(&_stamped)];
 
     // Keep webhook/gateway prompts aligned with channel behavior by injecting
     // workspace-aware system context before model invocation.
